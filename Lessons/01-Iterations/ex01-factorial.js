/**
 * This is factorial number function.
 *
 * @param {number} inputNumber - the input number.
 * @returns {number} the result after computing the factorial for the given input number.
 */
function factorial(inputNumber) {
    // Enter your code here!

    return 0;
}

// Call the function
console.log(factorial(10));